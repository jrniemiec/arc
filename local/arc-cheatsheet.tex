\documentclass[10pt,a4paper,landscape]{article}
\usepackage[margin=1cm]{geometry}
\usepackage{multicol}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{parskip}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

\definecolor{codebg}{gray}{0.93}
\definecolor{sectioncolor}{RGB}{30,90,160}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{codebg},
  frame=none,
  breaklines=true,
  breakatwhitespace=true,
  columns=fullflexible,
  keepspaces=true,
}

\titleformat{\section}{\normalfont\bfseries\color{sectioncolor}}{}{0em}{}[\titlerule]
\titleformat{\subsection}{\normalfont\small\bfseries}{}{0em}{}
\titlespacing{\section}{0pt}{4pt}{2pt}
\titlespacing{\subsection}{0pt}{3pt}{1pt}

\setlength{\columnsep}{0.6cm}
\pagestyle{empty}

\begin{document}
\begin{multicols}{3}

{\large\bfseries Arc Cheat Sheet}\\
{\small\textit{Personal Knowledge Engine}}

\section{TUI — Launch \& Panes}

\begin{lstlisting}
arc                   # launch TUI (no subcommand)

# Pane focus
Tab           Nav <-> Content toggle
Shift+Tab     always jump to Nav pane
Esc           jump to Command input
Alt+1         Nav pane
Alt+2         Content pane
Alt+3         Tab bar (Library / Agent / Stats)

# Split sub-panes
Ctrl+X        toggle global AskX pane
Ctrl+O        toggle Preview pane
Ctrl+L        toggle Scratch pane
\end{lstlisting}

\section{TUI — Keys}

\begin{lstlisting}
# Universal
j/k or Up/Down    move up/down
PgDn/Ctrl+D       page down
PgUp/Ctrl+U       page up
g/Home            go to top
G/End             go to bottom
Enter             select / expand / collapse
Space             expand / collapse
q / Ctrl+C        quit
?                 show key bindings help
/                 open command input

# Nav pane (articles/collections/workspaces)
r                 mark article read
u                 mark article unread
f                 toggle favorite
o                 open source URL in browser
v                 view article in terminal
D                 delete current item
U                 unlink from workspace
a                 move to attic
b                 restore from attic
Ctrl+R            refresh

# Content pane (article view)
h/Left or l/Right prev/next tab (Flash/Summary/Body/Cards)
s                 speak current tab (TTS)
[ ]               TTS slower / faster

# Content pane (workspace chat)
j/k               prev/next message box
s                 speak selected box
v                 expand/collapse box
#                 comment/uncomment box
x                 delete box

# Command input
Up/Down           recall command history
Ctrl+G            correct spelling/grammar
\end{lstlisting}

\section{TUI — Global Commands}

\begin{lstlisting}
/search <query>        full-text search
/article ingest <url>  add a new article
/stats                 library stats + cost
/models                list LLM profiles
/config                show resolved config
/config-edit           open config in $EDITOR
/scratch [msg]         workspace-local scratch
/Scratch [msg]         global scratch
/AskX <prompt>         global LLM query
/tags                  list all tags
/log                   open/close debug log tail
/help [group]          key/command help
\end{lstlisting}

\section{TUI — Chat Commands}

\begin{lstlisting}
# Active in workspace chat context:
/mode                  show grounding mode
/mode corpus-only      corpus only (no web)
/mode corpus-first     prefer corpus (default)
/mode open             unrestricted

/clear                 clear conversation history
/reload                rebuild corpus
/stats                 session token + cost
/system                show system prompt
/meta                  workspace details
/save [file]           save session to outcomes/
/new <name>            create workspace
/delete [name]         delete workspace
/rename <name>         rename workspace
/describe <text>       set workspace description
/populate [--hint ""] [--edit] [--dry-run]
/remove [--article slug] [--all-articles] ...

/resource-list
/resource-add <path|url> [--into <dir>] \
  [--as <name>] [--comment "..."]
/resource-view <name>
/resource-edit <name>
/resource-new <name>
/resource-remove <name>
/resource-save [filename]

/scratch [msg]         workspace scratch
/askX <prompt>         workspace LLM query
/AskX <prompt>         global LLM query
/models                list profiles
/help                  list chat commands
\end{lstlisting}

\section{Ingest}

\begin{lstlisting}
# Ingest from URL
arc ingest https://example.com/article

# Ingest from local file
arc ingest ~/papers/paper.pdf

# Ingest with collection tag
arc ingest <url> --collection ml

# Ingest without summary (flash only)
arc ingest <url> --no-summary

# Force re-ingest (already exists)
arc ingest <url> --force

# Dry run -> extract only, no write
arc ingest <url> --dry-run
\end{lstlisting}

\section{Extract / Summarize / Flash}

\begin{lstlisting}
# Extract text only
arc extract <url>

# Summarize piped text
arc extract <url> | arc summarize

# Summarize with style
arc summarize --style bullets <slug>

# Generate flash summary
arc flash <slug>

# Generate flashcards
arc flashcards <slug>

# Write results back to article dir
arc summarize --write <slug>
arc flash --write <slug>
\end{lstlisting}

\section{Read}

\begin{lstlisting}
# Read flash summary
arc read <slug>
arc read --flash <slug>

# Read full summary
arc read --summary <slug>

# Read flashcards
arc read --flashcards <slug>

# Read raw extracted text
arc read --body <slug>
\end{lstlisting}

\section{List \& Search}

\begin{lstlisting}
# List all articles
arc list

# List agent-ingested only
arc list --agent

# List from specific run
arc list --agent-run <run-id>

# List slugs only (for scripting)
arc list --slugs

# Full-text search
arc search "attention mechanism"

# Search with limit
arc search "golang concurrency" -n 20
\end{lstlisting}

\section{Reprocess \& Reindex}

\begin{lstlisting}
# Reprocess flash only
arc reprocess <slug> \
  --no-summary --no-flashcards --no-embed

# Reprocess missing only (skip existing)
arc reprocess <slug> --missing

# Rebuild search index
arc reindex
\end{lstlisting}

\section{Collections}

\begin{lstlisting}
# List / create / delete
arc collections list
arc collections create ml
arc collections delete ml

# Add / remove article
arc collections add <slug> ml
arc collections remove <slug> ml

# Show articles in collection
arc collections show ml

# Set description / rename
arc collections describe ml "ML papers"
arc collections rename ml machine-learning

# Read flashes / summaries across collection
arc collections read ml
arc collections read ml --summary

# AI-suggest collections
arc collections suggest
arc collections suggest --apply
\end{lstlisting}

\columnbreak

\section{Workspaces}

\begin{lstlisting}
# Create / list / show / delete
arc workspace new <name> "description"
arc workspace list
arc workspace show <name>
arc workspace delete <name>

# Add content
arc workspace add <name> --article <slug>
arc workspace add <name> --collection <slug>

# Resources
arc workspace resource add <name> <file>
arc workspace resource add <name> <url>

# System prompt
arc workspace system set <name> "You are..."
\end{lstlisting}

\section{Workspace Chat (CLI)}

\begin{lstlisting}
# Start interactive chat REPL
arc workspace chat <name>

# Chat with specific profile
arc workspace chat <name> --profile opus

# Clear history before starting
arc workspace chat <name> --clear
\end{lstlisting}

\section{Chat Config}

\begin{lstlisting}
# Show config
arc workspace chat-config <name>

# Set fields
arc workspace chat-config <name> \
  --profile sonnet
arc workspace chat-config <name> \
  --max-output-tokens 2048
arc workspace chat-config <name> \
  --max-user-messages 20

# Global template:  ~/.arc/config.json "chat"
# Per-workspace:
#   ~/.arc/workspaces/<name>/chat/chat.json
\end{lstlisting}

\section{Agent}

\begin{lstlisting}
# Run feed agent (one cycle)
arc agent run

# Dry run -> filter only, no ingest
arc agent run --dry-run

# Run with temporary focus
arc agent run \
  --focus "Kubernetes performance"

# Process decisions file
arc agent run \
  --decisions ~/.arc/agent/decisions-<id>.json

# View run history
arc agent log
arc agent log -n 5

# Per-feed signal/noise stats
arc agent stats

# Print digest for last run
arc agent digest
arc agent digest --run <run-id>
arc agent digest --tts
\end{lstlisting}

\section{Decisions File Workflow}

\begin{lstlisting}
# 1. Edit decisions file:
#    change action:"-" to action:"+"
vi ~/.arc/agent/decisions-<run-id>.json

# 2. Process overrides
arc-digest-rerun <run-id>

# Or interactive fzf review
arc-digest-review [run-id]
\end{lstlisting}

\section{Email Scripts}

\begin{lstlisting}
# Run agent (no email)
arc-digest

# Send digest email for run(s)
arc-digest-email
arc-digest-email <run-id>

# Run decisions + ingest overrides
arc-digest-rerun [run-id]

# Full morning chain (launchd)
arc-digest && arc-digest-email
\end{lstlisting}

\section{Other Commands}

\begin{lstlisting}
arc open <slug>          # open URL in browser
arc delete <slug>        # delete article
arc stats                # show library stats
arc profiles             # list LLM profiles + pricing
arc config               # print resolved config
arc mcp                  # start MCP server
arc init                 # initialise arc
\end{lstlisting}

\section{Key Paths}

\begin{lstlisting}
~/.arc/articles/<slug>/        # article data
~/.arc/collections/            # collection symlinks
~/.arc/workspaces/<name>/      # workspace root
  chat/chat.json               #   chat config
  chat/history.json            #   conversation history
  system.txt                   #   system prompt
  resources/                   #   uploaded files
  outcomes/                    #   saved transcripts
~/.arc/agent/runs.jsonl        # run history
~/.arc/agent/decisions-*       # decisions files
~/.arc/config.json             # main config
~/.arc/events.jsonl            # LLM cost events
\end{lstlisting}

\section{Global Flags}

\begin{lstlisting}
--config <file>       config file path
--data-root <dir>     arc data root
--log-level debug     verbose logging
--json                JSON output
\end{lstlisting}

\section{Environment Variables}

\begin{lstlisting}
ARC_ANTHROPIC_API_KEY  Anthropic API key
ARC_RECIPIENT          digest email recipient
ARC_FROM               digest email sender
ARC                    arc binary path
\end{lstlisting}

\end{multicols}
\end{document}
